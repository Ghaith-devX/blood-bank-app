import 'package:flutter/material.dart';

class GSizes {
  static const double spaceBetweenItems = 10;
  static const double spaceBetweenSections = 20;
  static const double defaultPadding = 20;

  static const EdgeInsets screenPadding = EdgeInsets.all(defaultPadding);
}
